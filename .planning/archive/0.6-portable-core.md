# 0.6 Portable Core — absorbed

**Status:** SHIPPED (in codebase) · **No separate tag**

## Original scope

| Item | Intent |
|------|--------|
| Optional `clientSessionId` | Host-neutral create |
| `taskId` authoritative | Poll/fetch without host session |
| Core ≠ host adapters | Shared SQLite + MCP; Cursor hooks optional |

## Evidence (shipped by 0.5.x)

- `clientSessionId` + deprecated `cursorConversationId` alias on create
- `unscoped` sentinel when session omitted
- Stdio MCP (Cursor) + remote MCP (ChatGPT) on same `HANDOFF_DB_PATH`
- Hook inject-session fix (2026-08-13)
- README + architecture document host-neutral contract

## Why no v0.6.0 portable-core tag

Scope was fully implemented before file resources work began. Keeping a empty milestone created docs/code drift (roadmap said “no read_file MCP” while `handoff_read_file` shipped).

## Superseded by

**0.6 Handoff Resources** — next tagged release. Portable contract is the foundation; resources are the feature.
