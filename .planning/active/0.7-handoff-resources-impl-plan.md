# v0.7 Handoff Resources — Implementation Plan

**PR:** `feat/0.7-handoff-resources` → `main`  
**Review:** APPROVE WITH CHANGES — architecture 9/10; 4 P0 amendments locked (2026-08-29)  
**Merge after:** v0.6 Worker Ops (recommended — unblocks E2E CONSENT/URL)

Phases A–E are **implementation order inside one PR**.

---

## Foundation (done)

- [x] Planning + ADRs
- [x] Immutable snapshot store
- [x] `handoff_read_file` + manifest
- [x] `npm run test:files` baseline

---

## Ship bar (locked)

| Item | Decision |
|------|----------|
| Production delivery | Native CDP attachment |
| MCP lazy read as ship substitute | **No** |
| Silent attach → MCP fallback | **Forbidden** |
| File limits | 10 × 256KB, 1MB/task |
| Chip verify | **Added-chip multiset** |
| Retry | **Only after cleanup + isClean()** |
| E2E | Distinct nonce per file + partial failure + text-only regression |

---

## Phase A — Resource safety ✅

Duplicate basename · secret-content guard · `test-files.ts`

---

## Phase B — Attachment primitive ✅

`composer-attach.ts` · `attachment-match.ts` · `transport/` · `test-attachment-match.ts`

---

## Phase C — Delivery state machine ✅

`worker.ts` fence after verify · `markDispatchFailed(permanent)` · ADR-004/006/007

---

## Phase D — E2E

Script: `scripts/e2e-files-attach.ts` (poll `/tasks/{id}` not `/status`)

| Scenario | Assert |
|----------|--------|
| D1 Happy | `NONCE_A` + `NONCE_B`; no MCP read |
| D2 Partial fail | No fence; cleanup |
| D3 Text-only | Normal dispatch |

**Gate:** v0.6 dashboard assign URL + auto-canary before live run.

---

## Phase E — Docs / release

- Update spec statuses
- `docs/architecture.md` delivery flow
- Tag `v0.7.0`

---

## PR checklist

- [x] `npm run typecheck`
- [x] `npm run test:files`
- [x] `npm run test:attachment-match`
- [ ] E2E D1–D3 green (live)
- [ ] Tag `v0.7.0`

---

## Agent handoff

```text
Branch: feat/0.7-handoff-resources
Spec: .planning/active/0.7-handoff-resources.md
Plan: this file
```
