# Database — use an absolute path if Cursor MCP is enabled at user level
# (other workspaces). Node does not expand ~ in paths.
HANDOFF_DB_PATH=/absolute/path/to/chatgpt-mcp/data/handoff.sqlite

# HTTP status API (used by Cursor stop hook)
HANDOFF_HTTP_PORT=8787

# Browser worker — attach via CDP to a Chrome that already has remote debugging.
# Chrome 136+ IGNORES --remote-debugging-port on the default user-data-dir
# (https://developer.chrome.com/blog/remote-debugging-port). You MUST use a
# non-standard dir (./scripts/start-chrome-cdp.sh → $HOME/chrome-chatgpt-debug)
# and sign into ChatGPT Pro in THAT window. Do not copy Default cookies.
#
# 1. ./scripts/start-chrome-cdp.sh
# 2. Log into ChatGPT Pro in the CDP window; copy worker chat URL here.
# 3. npm run worker
# Do NOT use launchPersistentContext / password automation.
CHATGPT_CDP_ENDPOINT=http://127.0.0.1:9222
CHATGPT_WORKER_URL=https://chatgpt.com/c/REPLACE_WITH_WORKER_CHAT_ID
CHATGPT_URL=https://chatgpt.com

# Polling intervals (ms)
DISPATCH_POLL_INTERVAL_MS=2000
DISPATCH_APPROVAL_TIMEOUT_MS=300000
RATE_LIMIT_BACKOFF_MS=300000,900000,1800000

# Cursor stop hook (seconds)
HANDOFF_WAIT_TIMEOUT=480
HANDOFF_POLL_INTERVAL=2

# Logging — absolute path so MCP stdio (any cwd) writes the same log
LOG_DIR=/absolute/path/to/chatgpt-mcp/logs

# Remote MCP (ChatGPT connector, exposed via a tunnel e.g. ngrok)
HANDOFF_REMOTE_MCP_PORT=8790
# Leave unset to auto-generate on first `npm run remote-mcp` (written back to .env)
HANDOFF_REMOTE_MCP_TOKEN=

# NOTE: Full MCP write tools (handoff_submit_result) require a ChatGPT plan that
# supports Developer Mode + MCP write (Business / Enterprise / Edu per OpenAI docs).
# Consumer Plus alone may only allow read/fetch MCP connectors.
